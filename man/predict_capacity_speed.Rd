% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_capacity.R
\name{predict_capacity_speed}
\alias{predict_capacity_speed}
\title{Predict Freezer Capacity and Sample Backlog}
\usage{
predict_capacity_speed(
  current,
  pending,
  planned,
  freezer_scale,
  processing_speed = 50000,
  tol = 1e-06
)
}
\arguments{
\item{current}{Data frame with \code{date}, \code{tube_size}, \code{tubes_count} (current stock).}

\item{pending}{Data frame with \code{date}, \code{tube_size}, \code{tubes_count} (pending backlog).}

\item{planned}{Data frame with \code{date}, \code{tube_size}, \code{tubes_count} (planned inflows).}

\item{freezer_scale}{Data frame with \code{tube_size} and \code{tubes_per_freezer}.}

\item{processing_speed}{Numeric, tubes per month the lab can process.}

\item{tol}{Numeric tolerance for solver stability.}
}
\value{
A tidy tibble with dates, tube sizes, cumulative tube and freezer counts, and phase labels.
}
\description{
Solves an optimization model to estimate processing and freezer needs
based on current, pending, and planned tube arrivals.
}
\examples{
\dontrun{
res <- predict_capacity(current, pending, planned, freezer_scale)
}
}
